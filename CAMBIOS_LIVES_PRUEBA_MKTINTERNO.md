# Lives · prueba MKT interno

- El selector de grupos de Úrsula ahora muestra únicamente grupos de la instancia `GRUPOS` con `purpose = live`.
- Esto evita mezclar grupos de Cursos/Workshop con los grupos de Lives.
- Para pruebas se usa `LIVE_PRUEBA_MKTINTERNO` -> `120363409439960903@g.us`.
- Más adelante se agregan los grupos reales como filas nuevas con `purpose = live`; no hace falta volver a cambiar el CRM.
